from setuptools import setup, find_packages

setup(
    name="document_corroboration_engine",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[
        # Dependencies will be installed from requirements.txt
    ],
    python_requires='>=3.8',
)
